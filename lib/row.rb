module TablePrint

  # note: if we have columns for "user.blogs.url" and "user.blogs.title", we only want to add one set of user.blogs to the stack
  #class Row
    # has cells
    # has child rows (acts as tree)
    # responsible for pulling children into its display if possible
  #end
end
